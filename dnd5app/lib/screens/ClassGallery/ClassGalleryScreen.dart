import 'package:flutter/material.dart';

class ClassGalleryScreen extends StatelessWidget {
  static const routeName = '/classGalleryScreen';
  
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Class Gallery!")
    );
  }
}